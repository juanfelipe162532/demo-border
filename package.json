{
  "name": "parent",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "copy:child": "rm -rf public/child && mkdir -p public && cp -r ../dist public/child",
    "build:with-child": "npm --prefix .. run build -- --mode embed && npm run copy:child"
  },
  "dependencies": {
    "vue": "^3.5.18"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^6.0.1",
    "typescript": "~5.8.3",
    "vite": "^7.1.2"
  }
}

